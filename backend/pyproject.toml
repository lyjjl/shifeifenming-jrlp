[project]
name = "jrlp-backend"
version = "1.3.0"
description = "今日老婆 JRLP 后端服务"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.116.1", "uvicorn>=0.35.0", "psutil>=7.0.0"]

[project.optional-dependencies]
tools = ["requests>=2.32.5"]

[dependency-groups]
dev = []
